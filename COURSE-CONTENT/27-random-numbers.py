import random

# print(help(random))

low = 1
high = 100
options = ("rock", "paper", "scissor")
cards = ["1", "2", "3", "4", "5", "A", "J", "K", "Q"]
# number = random.randint(low, high)
# number = random.random()  # used for random floating pointer number between 0 to 1
# option = random.choice(options) # used to choose a element from a collection
# print(option)
# random.shuffle(cards)
# print(cards)
